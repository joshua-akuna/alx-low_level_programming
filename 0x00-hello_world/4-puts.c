#include <stdio.h>

/**
 * main - Program entry point.
 *
 * Descriptiom: 'prints a string with the puts function'.
 *
 * Return: Returns 0 (if successful).
 */
int main(void)
{
	puts("\"Programming is like building a mutilingual puzzle");
	return (0);
}

#include "../lists.h"
/**
 * main - test code for the 100-first.c program.
 * Return: 0 Always.
 */
int main(void)
{
	printf("(A tortoise, having pretty good sense of a hare's nature, challenges one to a race.)\n");
	return (0);
}

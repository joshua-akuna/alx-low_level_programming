#include <stdio.h>
/**
 * main - prkints the number of arguments passed to this program.
 * @argc: int argument for the size of the array "argv".
 * @argv: a string array.
 * Return: Always 0.
 */

int main(int argc, char *argv[])
{
	(void) argv;
	printf("%d\n", argc);
	return (0);
}
